HI GUYS!
ill send my .env file on the teams, save it in this folder, its not saved on github

run:
python -m venv .venv

then:
.venv/scripts/activate

then:
pip install -r requirements.txt

to run flask server, run the command in to_run.bat, which is:
flask --app main run

run webpage as you usually would
